name: Keep Alive Hugging Face Space

on:
  schedule:
    # 매 시간 0분마다 실행 (UTC 기준)
    - cron: '0 * * * *'
  # 수동 실행 허용
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -I -s https://hwangjinwook-caretable.hf.space/
          echo "HF Space Pinged Successfully."
